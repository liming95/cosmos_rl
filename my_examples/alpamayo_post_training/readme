# tail model
python ./tail_model.py
## introduction
extract vlm model from the alpamayo1_5. Meanwhile, we also try to custome smaller size model for test by skip language_model.layers. config.text_config.num_hidden_layers
## issue
- config.text_config is the main parameters for building language model
- access the language_model by model.model.language_model.layers

# message building
python ./test_message_building.py
## introduction
a case to build the input data for the vlm model or alpamayo1_5.

# alpha_grpo.py
source $COSMOS_ROOT/.venv/bin/activate
source ./env.sh
./run.sh

## introduction
a case to execute RL-based post training for vlm model from th alpamayo1_5
## issue
- the vocab_size of model must be divided by 16
- input data need to be adjust because the image content in alpamayo1_5 is tensor, but the current cosmos-rl need a file. We have modify the bug in cosmos-rl to support the tensor image content.